# ===== Server =====
server.port=8080

# ===== MySQL =====
# Create the database first:  CREATE DATABASE eexam_db;
spring.datasource.url=jdbc:mysql://localhost:3306/eexam_db?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=@Achu2505@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===== JPA / Hibernate =====
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===== JWT =====
# 256-bit+ secret, change this in production
app.jwt.secret=eExamPlatformSuperSecretKeyChangeThisInProduction123456789
app.jwt.expiration-ms=86400000

# ===== Default admin account (created on first startup if it doesn't exist) =====
app.admin.default-username=admin
app.admin.default-password=Admin@123
